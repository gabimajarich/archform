{
  "buildCommand": "cd archform && npm install && npm run build",
  "outputDirectory": "archform/dist",
  "installCommand": "echo skip",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
